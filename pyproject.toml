[project]
name = "eastvantage-address-book-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic",
    "geopy",
    "pytest>=9.0.2",
    "httpx>=0.28.1",
]
